using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestPair : MonoBehaviour
{

    TestCo.pair[] Copy;
    // Start is called before the first frame update
    /*
    void Start()
    {
        Copy = new TestCo.pair[5];
        for (int i = 0; i < 5; i++){
            Copy[i] = TestCo.Instance.Arr[i];
        }

        for (int i = 0; i < 5; i++)
            Debug.Log("TestCo firt" + Copy[i].first + "  Sec " + Copy[i].second);

    }
    */
    // Update is called once per frame
    void Update()
    {
        
    }
}
